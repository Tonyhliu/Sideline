const React = require('react');
// const StoryActions = require('../')
// const StoryIndexItem = require('./story_index_item');

const StoriesIndex = React.createClass({
  getInitialState() {
    // return({ stories: [] });
  },

  _storyChange() {
    // this.setState({ stories: StoriesStore.fetchPosts() });
  },

  componentDidMount() {
    // this.storiesListener = StoriesStore.addListener(this._storyChange);
  },

  render() {

  }
});
